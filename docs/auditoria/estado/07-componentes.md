# Frontend — Componentes de domínio (`src/components/` exceto `ui/` e `__tests__/`)

## 1. Cobertura

**Escopo medido:** 272 arquivos `.ts/.tsx` / **36.496 linhas** em 43 pastas de domínio
(exclui `ui/` = 58 arq./3.809 linhas e `__tests__/` = 224 arq./20.139 linhas).

| Método | O que foi feito |
|---|---|
| **Lido integralmente** (33 arquivos) | `auth/LoginForm.tsx`, `forms/*` (4), `settings/InlineTabs.tsx` (regiões 20-135 e 275-430), `ponto/PontoLeaderboard.tsx`, `ponto/GestaoRegistrosPonto.tsx` (1-100), `ponto/PontoClockRegister.tsx` (150-240), `folha/FolhaDashboard.tsx` (1-45), `esocial/ESocialAIInsights.tsx`, `esocial/ESocialConciliacao.tsx` (1-40), `esocial/ESocialComplianceScore.tsx`, `esocial/ESocialAuditDialog.tsx` (30-90), `esocial/S2205AlteracaoCadastral.tsx`, `esocial/S1200Remuneracao.tsx`, `admissoes/OnboardingDashboard.tsx` (1-45), `metabase/MetabaseEmbed.tsx` (50-90), `dashboard/AnalyticsSection.tsx` (95-115), `dashboard/MorningBriefing.tsx` (48-75), `dashboard/WorkforceHealthScore.tsx` (1-40), `layout/MainLayout.tsx`, `layout/index.ts`, `sst/SSTNRsTab.tsx`, `sst/SSTProgramasTab.tsx` (70-90), `colaborador-detalhes/EstagiarioTab.tsx` (1-30), `premiacoes/RewardsSimulator.tsx` (205-225), `login/LockoutMessage.tsx`, `obrigacoes/GuiasTable.tsx` (1-25), `dashboard/analytics/widgets.tsx` (1-30), `integracoes/ConfigPanels.tsx` (grep de persistência). |
| **Amostrado por varredura estática** (todos os 272) | (a) grafo de referências arquivo→consumidor (script de detecção de órfãos, 2 passadas: com e sem barris `index.ts`); (b) mapa domínio→página consumidora; (c) extração de todos os `.from('tabela')` de `components/`, `pages/`, `hooks/`, `services/` (256 nomes distintos) e checagem de existência/volumetria **contra o banco de produção via MCP (somente leitura)**; (d) grep de `Math.random`, `mock/dummy/fixture/TODO/EM BREVE`, `onClick={() => {}}`, `<Button>` sem handler, séries numéricas hardcoded, hints PostgREST `colaboradores!*_fkey`. |

**Não executei** build/typecheck/lint/testes — `node_modules` ausente (conforme briefing). Nenhum arquivo do repositório foi editado.

---

## 2. Tabela de funcionalidades (por domínio)

| Funcionalidade | Classificação | Evidência (arquivo:linha / objeto de banco) | O que falta |
|---|---|---|---|
| **pcs/** (Plano de Cargos e Salários, 6 comp./799 linhas) | `MORTO_OU_ABANDONADO` | `components/pcs/*` → `pages/PlanoCargosSalariosPage.tsx` → `hooks/usePcs.ts:32` → `services/pcsService.ts:31` `.from('pcs_planos')`. **As tabelas `pcs_planos`, `pcs_fatores`, `pcs_grades`, `pcs_avaliacoes_cargo`, `pcs_pesquisa_salarial` NÃO EXISTEM em `public`** (verificado: `information_schema.tables` retorna 0 linhas para todas as 5). `src/types/pcs.ts:10` deriva os tipos de `Database['public']['Tables']['pcs_planos']`, que existe só no `types.ts` gerado. | Migration nunca aplicada. Toda query retorna PGRST205 em runtime; a página inteira quebra. |
| **Vínculo de benefícios** (`colaborador-detalhes/BeneficiosTab`, `beneficios/BeneficiosDashboard`, `beneficios/BeneficioHistorico`) | `IMPLEMENTADO_PARCIAL` (fio quebrado) | `components/colaborador-detalhes/BeneficiosTab.tsx:3,20` → `hooks/useBeneficiosColaborador.ts:14,29,49` `.from('beneficios_colaborador')` = **0 linhas**. Dado real está em **`beneficios_colaboradores` = 74 linhas** (2ª maior tabela de negócio) e **nenhum arquivo de `src/` ou `supabase/functions/` referencia esse nome** (grep global sem resultado). Terceira variante `colaborador_beneficios` = 0 linhas, usada em `services/tabelas/beneficiosService.ts:39`. | Três tabelas paralelas; o código lê as duas vazias. 74 registros reais invisíveis na UI e no cálculo de folha (`services/folha/calculoLoteService.ts:139`). |
| **ponto/GestaoRegistrosPonto** (tela principal de gestão de ponto) | `IMPLEMENTADO_PARCIAL` (query quebrada) | `components/ponto/GestaoRegistrosPonto.tsx:37-38`: `.from('registros_ponto').select('*, colaborador:colaboradores!batidas_ponto_colaborador_id_fkey(...)')`. Verificado em `pg_constraint`: `registros_ponto` só possui `registros_ponto_colaborador_id_fkey`; o nome `batidas_ponto_colaborador_id_fkey` pertence a **outra tabela**. PostgREST → PGRST200 → `throw error` (linha 43). `registros_ponto` tem **120 linhas** (maior tabela de negócio). | Hint de FK residual de rename incompleto. A listagem principal nunca renderiza os 120 registros. |
| **ponto/** (restante: ClockRegister, Charts, Streak, Adjustment, AuditTimeline, Inconsistency) | `IMPLEMENTADO_PARCIAL` | `PontoClockRegister.tsx:153,195` (storage `ponto-biometria` + fila offline via `pontoOfflineService`), `PontoCharts.tsx:27` e `PontoStreakCard.tsx:24` leem `registros_ponto` (120). Mas `GestaoRegistrosPonto.tsx:53` e `dashboard/MorningBriefing.tsx:61` leem `batidas_ponto` = **0 linhas**. `conformidade_ponto_logs`, `trilha_auditoria_ponto` = 0. | Cisma `batidas_ponto` (0) × `registros_ponto` (120): dois modelos concorrentes convivendo, com `services/batidasPontoService.ts` inteiro apontando para a vazia. |
| **ponto/PontoLeaderboard** | `SUGERIDO_OU_INICIADO` (dado 100% fictício) | `components/ponto/PontoLeaderboard.tsx:6-12` — `MOCK_LEADERBOARD` com nomes inventados (Ana Silva, João Santos, Maria Oliveira…). Zero query. Montado em produção: `pages/PontoPage.tsx:42,423`. | Nenhuma fonte de dado. Exibe pessoas que não existem em `colaboradores` (13 reais). |
| **esocial/** (22 comp./1.898 linhas) | `SUGERIDO_OU_INICIADO` | `services/esocialService.ts:52,121,208` `.from('esocial_eventos')` = **0 linhas**; `configuracoes_esocial` = 0; `certificados_digitais` = 0; `esocial_transmissao_logs` = 0. Os 14 viewers S-xxxx (`ESocialEventViewer.tsx:1-13`) são puramente de leitura de `dados: any` (ex.: `S2205AlteracaoCadastral.tsx:6`), sem submissão. | Nenhum evento jamais gerado/transmitido. Domínio inteiro apontando para tabelas vazias. |
| **esocial/ESocialAIInsights** | `SUGERIDO_OU_INICIADO` (dado fictício) | `ESocialAIInsights.tsx:7-20` — array `insights` hardcoded, inclui colaborador fictício "João Silva"; `:58` botão "Ver todos os insights (12)" com contagem hardcoded e sem `onClick`. Título "Insights da IA Lovable" (`:27`). Montado: `pages/ESocialPage.tsx:25`. | Não há IA nem query. |
| **esocial/ESocialConciliacao** | `SUGERIDO_OU_INICIADO` (dado fictício) | `ESocialConciliacao.tsx:13-40` — `mockConciliacao` com valores fixos (R$ 12.500,00; divergência R$ 5,20) renderizado em `:119`. Montado: `pages/ESocialPage.tsx:26`. | Conciliação sistema×governo é encenação. |
| **esocial/ESocialAuditDialog** | `IMPLEMENTADO_PARCIAL` | `ESocialAuditDialog.tsx:41` comentário `// Simulated audit logic`; `:28-38` barra de progresso teatral (`+5` a cada 100ms). A heurística em `:42-72` é real, mas opera sobre `eventos` vindos de `esocial_eventos` = 0 linhas. Rotulado "Auditoria Proativa IA" (`:87`). | Nenhuma IA; sem dado sempre cai em "Todos os eventos … em conformidade" (`:70`). |
| **settings/InlineTabs — PreferenciasTab, NotificacoesTab, FolhaConfigTab, PontoConfigTab** | `SUGERIDO_OU_INICIADO` (esqueleto sem lógica) | `settings/InlineTabs.tsx:20-134`. Nenhum `useState`/`useQuery`/`useMutation`. Campos `FormField`/`FormSwitch` não controlados, só `defaultValue` (`:76-77`, `:112`). Botões "Salvar Alterações" (`:41`), "Salvar Preferências" (`:77`), "Salvar Configurações" (`:102`), "Atualizar Regras" (`:127`) **sem `onClick`**. Montados em produção: `pages/ConfiguracoesPage.tsx:75-78`. | 4 de 15 abas de Configurações não persistem nada. Tabela `configuracoes` = 0 linhas. |
| **settings/InlineTabs — IntegracoesTab** | `IMPLEMENTADO_PARCIAL` | `:281-287` consulta `integracoes` = **0 linhas** (com swallow de `42P01`). Badge "Conectado" do Bitrix24 é **hardcoded** (`:302`), não deriva da query. Botão "Webhook de Mudanças" (`:322`) sem `onClick`. RD Station e Google Workspace = `EM BREVE` (`:341`, `:360`). | Status de integração mentiroso; 2 de 3 cards são placeholder. |
| **settings/** (restante: CamposCustomizados, IPBlocking, SystemHealth, GlobalAuditLog, UserRoles, Beneficios, Empresa, MFASetup, AlertasKpi, WebhooksLogs, LogsIntegracoes) | `IMPLEMENTADO_PARCIAL` | Código com CRUD real, mas tabelas vazias: `campos_customizados`=0 (`CamposCustomizadosTab.tsx:50,79,83`), `blocked_ips`=0, `user_mfa`=0, `config_alertas_indicadores`=0, `webhooks_logs`=0, `logs_integracoes`=0, `configuracoes`=0. Com dado: `user_roles`=4, `audit_log`=281. | Fluxos nunca exercitados em produção. |
| **folha/FolhaDashboard** | `SUGERIDO_OU_INICIADO` (dado fictício) | `folha/FolhaDashboard.tsx:13-27` — `trendData` (6 meses, R$ 45k-52k) e `compositionData` (75/15/10%) hardcoded. Prop `competencia` é **declarada e nunca usada** (só nas linhas 10 e 29). Montado: `pages/FolhaPagamentoPage.tsx:236`. | Dois de três gráficos do dashboard de folha são fixos; `folhas_pagamento`=4, `folha_itens`=12 existem mas não alimentam nada aqui. |
| **folha/** (restante: KPIs, Pipeline, Composicao, CalculoFolhaWizard, CNAB, PagamentoBancario, Rubricas, Simuladores, AuditTimeline, FGTSDigital, ESocialSync, MetricasFolha) | `IMPLEMENTADO_PARCIAL` | `CalculoFolhaWizard.tsx:80,111,120` grava em `folha_auditoria` (**0 linhas**) e `folha_itens` (12). `RubricasDialog.tsx:52,68` usa `rubricas_folha` (24 ✓). `FolhaESocialSync.tsx:41-42` inicializa eventos com `total: 0, processed: 0` e `esocial_eventos`=0. `cnab_remessas`=3, `pix_lotes`=2. | Persistência existe, volume é de teste (4 folhas / 12 itens / 1 empresa). |
| **layout/** — primitivos mortos | `MORTO_OU_ABANDONADO` | **11 arquivos, 885 linhas, zero uso em JSX fora da própria pasta** (verificado com `grep -rn "<Nome[ />]"`): `Stack.tsx`(5), `Flex.tsx`(5), `Container.tsx`(97), `Row.tsx`(109), `Column.tsx`(121), `Grid.tsx`(104), `SectionHeader.tsx`(5), `Breadcrumb.tsx`(18), `PageContainer.tsx`(127), `NavItem.tsx`(94), `EmpresaSwitcher.tsx`(246). `EmpresaSwitcher` é exportado por `layout/index.ts:6` mas nenhum arquivo o importa. As únicas referências restantes são JSDoc (`Grid.tsx:74-75`, `Column.tsx:85`) e testes. | Nenhum caminho de execução. `PageLayout` (256 refs) e `Breadcrumbs` (plural, 6 refs) são os vivos. |
| **layout/** — parte viva (MainLayout, AppSidebar, Header, PageLayout, Breadcrumbs, MobileBottomNav, PageTransition, SidebarFooter, EntityPageContainer, Footer, BackButton) | `IMPLEMENTADO_TOTAL` | `App.tsx:2` → `MainLayout.tsx:5` (`Sidebar` = alias de `AppSidebar` em `layout/index.ts:2`), `:57` render; `Header.tsx:8,42,91`. Dado real: `empresas`=1, `user_roles`=4, `profiles`=2. | — |
| **auth/LoginForm** | `MORTO_OU_ABANDONADO` | `components/auth/LoginForm.tsx:13` — único consumidor é `components/__tests__/LoginForm.test.tsx`. Sem lógica de autenticação: `handleSubmit` só chama `onSubmit?.(email, password)` (`:20`). O login real é `pages/LoginPage.tsx` + `components/login/LockoutMessage.tsx`. | Refactor abandonado: `auth/` substituído por `login/`+`LoginPage`, mas `auth/` continua no repo com teste dedicado. |
| **Órfãos com teste dedicado** (vanity coverage) | `MORTO_OU_ABANDONADO` | Sem consumidor de produção, **com** arquivo de teste: `auth/LoginForm.tsx`(63) ↔ `LoginForm.test.tsx`; `ponto/AssinarEspelhoDialog.tsx`(176) ↔ `AssinarEspelhoDialog.test.tsx`; `notifications/NotificationList.tsx`(33) ↔ `NotificationList.test.tsx`; `notifications/NotificationItem.tsx`(33) ↔ `NotificationItem.test.tsx`; `ferias/FeriasSaldoReport.tsx`(85) ↔ `FeriasSaldoReport.test.tsx`; `layout/PageContainer.tsx` ↔ `PageContainer.test.tsx`; `layout/NavItem.tsx` ↔ `LayoutPrimitives.test.tsx`; `layout/Container.tsx` ↔ `Container.test.tsx`; `layout/SectionHeader.tsx` ↔ `SectionHeader.test.tsx`; `layout/Grid.tsx`/`Section` ↔ `LayoutGridSection.test.tsx`. | Testes que não protegem nada em produção. |
| **metabase/MetabaseEmbed** | `MORTO_OU_ABANDONADO` + dado fictício | 423 linhas, **zero importadores** (grep global: só menções em JSDoc de `services/metabaseService.ts:8,104`). `MetabaseEmbed.tsx:60-90` — `FALLBACK_DATA` com headcount inventado (Produção=67, Operações=45; real: `colaboradores`=13) e custo de folha R$ 485-523 mil (real: 4 folhas). | Componente nunca montado; se fosse, exibiria números falsos. |
| **i18n/LanguageSelector** | `MORTO_OU_ABANDONADO` | `components/i18n/LanguageSelector.tsx` (76 linhas) — **nenhuma referência em todo o `src/`**, nem em testes. | i18n nunca foi ligado. |
| **notifications/** | `IMPLEMENTADO_PARCIAL` | Vivos: `NotificationBell` (`layout/AppSidebar.tsx:11`), `NotificationCenter` (`layout/Header.tsx:91`). Mortos: `NotificationList.tsx`, `NotificationItem.tsx`. Tabela `notificacoes` = **0 linhas** (`dashboard/AnalyticsSection.tsx:148,177,183`). | Sino de notificações nunca teve o que mostrar. |
| **dashboard/AnalyticsSection** (1.010 linhas, a maior) | `IMPLEMENTADO_PARCIAL` | `:93` lê `colaboradores` (13 ✓); `:148` lê `notificacoes` (**0**). `:100-106` `passivoTrend` com série hardcoded (Jan-Mai, R$ 45k-61k) — **variável declarada e nunca usada** (grep: única ocorrência é a própria declaração). Montado: `pages/DashboardPage.tsx:302`, `pages/DashboardExecutivoPage.tsx:339`. | Resíduo de mock não removido; central de notificações vazia. |
| **dashboard/MorningBriefing** | `IMPLEMENTADO_PARCIAL` | `MorningBriefing.tsx:55-63` — 8 queries paralelas. Com dado: `colaboradores`(13), `ferias`(12), `afastamentos`(1), `admissoes`(8). **Sem dado**: `exames`(0, `:59`), `batidas_ponto`(0, `:61`), `esocial_eventos`(0, `:63`). `:68` `esocialHealth` cai em `100` quando total=0 → **sempre 100%**. | Métrica de saúde eSocial vacuamente perfeita. |
| **admissoes/OnboardingDashboard** | `IMPLEMENTADO_PARCIAL` | KPIs derivam da prop `admissoes` (`:9-18`, tabela `admissoes`=8 ✓), mas `:21-27` `timeToHire` é **hardcoded** com comentário `// Time to hire (mocked for demo)` e alimenta o gráfico "Tempo Médio de Contratação". | Um gráfico de quatro é falso. |
| **admissoes/ + admissao/ + onboarding/** | `IMPLEMENTADO_PARCIAL` | `pages/AdmissoesPage.tsx` monta `AdmissoesKanban`, `NovaAdmissaoDialog`, `DetalhesAdmissaoDialog`. Duas pastas paralelas: `admissao/AdmissaoChecklist` é importado por `admissoes/DetalhesAdmissaoDialog.tsx:7`; `admissao/SignaturePad` por `pages/ContratacaoPage.tsx:37` (e existe um segundo `sst/SignaturePad.tsx`). Tabelas duplicadas: **`onboarding_tarefas`=27** (`services/tabelas/rhService.ts:122`) × **`tarefas_onboarding`=24** (`components/admissoes/OnboardingPageContent.tsx:34` e `pages/OnboardingPage.tsx:39`) — dois modelos de tarefa de onboarding, ambos populados por caminhos distintos. | Modelagem duplicada não consolidada; `SignaturePad` duplicado em 2 pastas. |
| **obrigacoes/** (4 comp.) | `SUGERIDO_OU_INICIADO` | `pages/ObrigacoesFiscaisPage.tsx:48-54` — todas as fontes vazias: `dctfweb_declaracoes`=0, `sefip_arquivos`=0, `guias_fgts`=0, `guias_inss`=0, `certificados_digitais`=0, `simulacoes_fiscais`=0. Componentes são apresentação pura sobre props (`obrigacoes/GuiasTable.tsx:19-24`). | Nenhuma guia/declaração jamais gerada. |
| **relatorios/** | `IMPLEMENTADO_PARCIAL` | `RelatoriosAgendadosTab.tsx:33,46,63` usa `relatorios_agendados` = **0 linhas**. `pages/RelatoriosPage.tsx` exporta de `colaboradores`/`ferias`/`admissoes`/`desligamentos`/`folhas_pagamento` (com dado). | Agendamento de relatórios nunca usado. |
| **portal/** (5 comp./1.007 linhas) | `IMPLEMENTADO_PARCIAL` | `PortalDocumentosTab.tsx:140` `.from('documentos')` = **0**; `documentos_assinatura`=0; `portal_notificacoes_settings`=0; `holerites`=0; `comunicados`=0. Com dado: `profiles`=2, `ferias`=12, `registros_ponto`=120, `sst_regimento_documentos`=1, `sst_regimento_assinaturas`=1. `PortalMeusDadosTab.tsx:92` botão "Solicitar Alteração de Dados" **sem `onClick`**. | Abas Documentos e Financeiro sem substrato; um botão morto. |
| **sst/** (11 comp.) | `IMPLEMENTADO_PARCIAL` | Com dado: `sst_programas`=4, `sst_riscos_ambientais`=4, `sst_incidentes`=2, `sst_extintores`=5, `sst_cat`=1, `sst_ltcat_laudos`=1, `sst_ordens_servico`=1. Falhas: `SSTIncidentesTab.tsx:81` botão "Exportar CAT" → `toast.info('Funcionalidade de exportação em breve')`; `SSTProgramasTab.tsx:78` botão "Renovar" **sem `onClick`**; `SSTNRsTab.tsx:7-15` é lista estática de NRs (referência legal — aceitável, mas sem vínculo com `sst_*`). | Duas ações de UI mortas; exportação de CAT não existe. |
| **exames/** (4 comp.) | `IMPLEMENTADO_PARCIAL` (fio quebrado) | `pages/ExamesPage.tsx:7` monta `ExameKPIs/ExameTable/ExameFormDialog/ExameTipoChart` sobre `.from('exames')` = **0 linhas**. O dado real de exame está em **`exames_agendamentos`=6** e **`asos`=6**, que a página não consulta. | Módulo lê a tabela errada; 12 registros reais invisíveis. |
| **epis/** (4 comp.) | `IMPLEMENTADO_PARCIAL` | `pages/EPIsPage.tsx:9` monta os 4 componentes; tabelas `epis`=8, `epis_entregas`=8, `epis_fichas`=1 (com dado). | Volume de teste. |
| **ferias/** (21 comp./2.836 linhas) | `IMPLEMENTADO_PARCIAL` | Com dado: `ferias`=12, `periodos_aquisitivos`=12, `ferias_audit_log`=12. Sem dado: `ferias_coletivas`=0 (`FeriasColetivasTab`), `ferias_programacao` (0 — `hooks/ferias/useProgramacaoFerias.ts:73`, subpasta `ferias/programacao/` com 4 componentes). **Órfão:** `ferias/FeriasSaldoReport.tsx` (85 linhas, só o teste importa). | Programação e férias coletivas nunca exercitadas; 1 componente morto. |
| **desligamentos/** (7 comp.) | `IMPLEMENTADO_PARCIAL` | `pages/DesligamentosPage.tsx:29` importa via barril os 7. `desligamentos`=2, `tipos_desligamento`=5. `homologacoes_rescisao`, `historico_rescisoes` = 0. | 2 desligamentos reais; `TurnoverChart` sobre base estatisticamente vazia. |
| **afastamentos/** (5 comp.) | `IMPLEMENTADO_PARCIAL` | `pages/AfastamentosPage.tsx` monta os 5. `afastamentos`=**1**, `config_afastamentos`=6, `cid10`=6. `documentos_afastamento`, `prorrogacoes_afastamento` = 0. | 1 registro. |
| **descontos/** (5 comp.) | `IMPLEMENTADO_PARCIAL` | `pages/DescontosPage.tsx` — `adiantamentos_salariais`=4, `emprestimos_consignados`=7 (com dado); `FinancialSummaryCards.tsx`, `NewLoanDialog.tsx:39` com margem consignável real. | Volume de teste. |
| **medidas-disciplinares/** (8 comp./1.352 linhas) | `SUGERIDO_OU_INICIADO` | `pages/MedidasDisciplinaresPage.tsx:7` monta 7 componentes; `services/medidasDisciplinaresService.ts:24,39,158,248` → `medidas_disciplinares`=**0**, `medidas_disciplinares_workflow_log`=0, `medidas_disciplinares_integracao`=0, `medidas_ciencia_tokens`=0. (Obs.: `:118` e `:197/:238` são **buckets de storage**, não tabelas — não é bug.) | Nenhuma medida jamais registrada. |
| **premiacoes/** (3 comp./744 linhas) | `IMPLEMENTADO_PARCIAL` | `services/premiacoesService.ts` → com dado: `premiacoes_campanhas`=3, `premiacoes_regras`=2, `premiacoes_auditoria`=8. Sem dado: `premiacoes_pagamentos`=0, `premiacoes_roi_cenarios`=0, `notificacoes`=0. `RewardsSimulator.tsx:215` botão "Excluir snapshots" com **`onClick={() => {}}`**. | Nenhum pagamento de premiação; 1 handler vazio. |
| **avaliacao/** (3 comp.) | `IMPLEMENTADO_PARCIAL` | `services/avaliacaoService.ts` → `ciclos_avaliacao`=2 (✓); `feedbacks_360`=0, `metas_okrs`=0, `competencias_config`=0, `pdi_plano_desenvolvimento`=0. `NineBoxMatrix` e `PerformanceDashboard` operam sobre essas vazias. | Matriz 9-box sem insumo. |
| **workflows/WorkflowDesigner** (637 linhas) | `IMPLEMENTADO_PARCIAL` | `pages/WorkflowsPage.tsx:18,55,61` → `workflowService`; `workflows_definicoes`=1, `workflows_execucoes`=8 (com dado). `WorkflowDesigner.tsx:96` `Math.random()` é geração de id (legítimo). | Uso mínimo (1 definição). |
| **admin/ + admin/telemetry/** (8 comp.) | `IMPLEMENTADO_TOTAL` | `pages/AdminTelemetriaPage.tsx` / `AdminOperacaoPage.tsx` montam `TelemetryCharts/Filters/StatsCards/Table/TopOffenders`, `HealthTrendCard`, `UnifiedAuditSection`. Dado real: `query_telemetry`=**265**, `audit_log_unified`=**512**, `audit_log`=281. | — (é o único domínio com volume não-trivial e caminho completo). |
| **colaborador-detalhes/** (16 tabs/1.558 linhas) | `IMPLEMENTADO_PARCIAL` | `pages/ColaboradorDetalhesPage.tsx:22` importa via barril. Tabs bem cabeadas (ex.: `EstagiarioTab.tsx:27-28` com hook + mutation). Com dado: `contas_bancarias`=12, `periodos_aquisitivos`=12, `asos`=6. **Vazias (9 de 16 tabs)**: `dados_estagiario`=0, `dados_estrangeiro`=0, `dependentes`=0, `formacoes_academicas`=0, `historico_salarial`=0, `historico_contratos`=0, `contatos_emergencia`=0, `anotacoes_colaborador`=0, `documentos_pessoais_arquivos`=0; + `BeneficiosTab` lê a tabela errada (ver acima). | Maioria das abas de ficha do colaborador nunca preenchida. |
| **dashboard/** restante (MetricCard, DonutChart, MiniSparkline, AnimatedNumber, BarChartWidget, QuickActionsGrid, EventTimeline, WorkforceHealthScore, DashboardHeader, SectionHeader, analytics/widgets) | `IMPLEMENTADO_PARCIAL` | `WorkforceHealthScore.tsx:18-31` calcula score a partir de props reais; `analytics/widgets.tsx:22` usa `viewsService`. `EventTimeline.tsx:74` filtra `log.tabela === 'batidas_ponto'` (tabela vazia). | Componentes de apresentação corretos; insumo parcialmente vazio. |
| **contratos/, documents/, integracoes/, recrutamento/, organograma/, seguranca/, departamentos/, colaboradores/, empresa/, contratacao/, beneficios/, onboarding/** | `IMPLEMENTADO_PARCIAL` | Todos com consumidor de página confirmado. Com dado: `contrato_templates`=5, `contrato_token_eventos`(via hooks), `candidatos`=5, `candidaturas`=5, `vagas`=3, `recrutamento_entrevistas`=2, `departamentos`=10, `cargos`=14, `colaboradores`=13, `empresas`=1. Sem dado: `documentos`=0, `documentos_historico`=0, `integracoes`=0, `logs_integracoes`=0, `security_alerts`=0, `blocked_ips`=0. | Cadastros básicos ok; documentos/integrações/segurança vazios. |

---

## 3. Achados graves

**1. [CRÍTICO] Módulo PCS inteiro aponta para tabelas que não existem em produção.**
`components/pcs/*` (6 arquivos, 799 linhas) → `pages/PlanoCargosSalariosPage.tsx` → `hooks/usePcs.ts:32` → `services/pcsService.ts:31`. Consulta ao `information_schema.tables` de produção: **`pcs_planos`, `pcs_fatores`, `pcs_grades`, `pcs_avaliacoes_cargo`, `pcs_pesquisa_salarial` não existem**. São as **únicas 5** entre os 256 nomes de tabela referenciados em `src/` que estão ausentes. `src/types/pcs.ts:10` deriva os tipos de `integrations/supabase/types.ts` (5 ocorrências de `pcs_planos`), ou seja, o `types.ts` gerado está dessincronizado do banco real. Toda operação do módulo falha em runtime.

**2. [CRÍTICO] 74 registros reais de benefícios invisíveis: três tabelas paralelas, o código lê as duas vazias.**
`beneficios_colaboradores` = **74 linhas** (2ª maior tabela de negócio do sistema) — **grep global em `src/` e `supabase/functions/` não encontra uma única referência a esse nome**. Todo o caminho de benefícios usa `beneficios_colaborador` (**0 linhas**): `hooks/useBeneficiosColaborador.ts:14,29,49`, `services/beneficioService.ts:96,109,119`, `services/calculoBeneficiosService.ts:41,199`, `services/folha/calculoLoteService.ts:139`. Uma terceira variante, `colaborador_beneficios` (**0 linhas**), é usada em `services/tabelas/beneficiosService.ts:39`. Componentes afetados: `colaborador-detalhes/BeneficiosTab.tsx:20`, `beneficios/BeneficiosDashboard`, `beneficios/BeneficioHistorico`, e o cálculo de benefícios na folha.

**3. [CRÍTICO] Tela principal de Gestão de Ponto quebra por hint de FK inválido.**
`components/ponto/GestaoRegistrosPonto.tsx:37-38`:
```
.from('registros_ponto')
.select('*, colaborador:colaboradores!batidas_ponto_colaborador_id_fkey(nome_completo, cargo, departamento, foto_url)')
```
Verificado em `pg_constraint`: `registros_ponto` possui apenas `registros_ponto_colaborador_id_fkey`; `batidas_ponto_colaborador_id_fkey` pertence a `batidas_ponto`. PostgREST retorna PGRST200 e a linha 43 faz `throw error` — a listagem nunca renderiza as **120 linhas** de `registros_ponto`. É o único hint `colaboradores!*_fkey` incorreto entre os 18 encontrados no repo (todos os demais foram validados contra `pg_constraint`).

**4. [ALTO] Cisma de modelo de ponto: `batidas_ponto` (0 linhas) × `registros_ponto` (120 linhas), ambos ativos.**
`services/batidasPontoService.ts` inteiro (linhas 5-63) opera sobre a vazia. Também leem a vazia: `GestaoRegistrosPonto.tsx:53`, `dashboard/MorningBriefing.tsx:61`, `hooks/useExecutiveDashboard.ts:45`, `hooks/useRealtimeDashboard.ts:14`, `dashboard/EventTimeline.tsx:74`. Consequência: contadores de "pontos hoje" e a geo-analítica sempre em zero.

**5. [ALTO] Quatro abas de Configurações são UI puramente decorativa em produção.**
`settings/InlineTabs.tsx:20-134` — `PreferenciasTab`, `NotificacoesTab`, `FolhaConfigTab`, `PontoConfigTab`: nenhum estado, nenhuma query, nenhuma mutation; campos não controlados; botões "Salvar Alterações"/"Salvar Preferências"/"Salvar Configurações"/"Atualizar Regras" (`:41`, `:77`, `:102`, `:127`) **sem `onClick`**. Montadas em `pages/ConfiguracoesPage.tsx:75-78`. O usuário altera "Tolerância de Atraso", "Exigir Geolocalização", "Dia de Fechamento" e nada é gravado (`configuracoes` = 0 linhas).

**6. [ALTO] Dado fictício exibido como real em telas de produção.**
- `ponto/PontoLeaderboard.tsx:6-12` — 5 colaboradores inventados com scores e streaks, montado em `pages/PontoPage.tsx:423`. A empresa tem 13 colaboradores reais, nenhum com esses nomes.
- `folha/FolhaDashboard.tsx:13-27` — série de custo de folha (R$ 45k-52k, 12-14 colaboradores) e composição 75/15/10% hardcoded; prop `competencia` recebida e **nunca usada**; montado em `pages/FolhaPagamentoPage.tsx:236`.
- `esocial/ESocialConciliacao.tsx:13-40` — conciliação sistema×governo com valores fixos e uma "divergência" de R$ 5,20 fabricada; montado em `pages/ESocialPage.tsx:26`.
- `esocial/ESocialAIInsights.tsx:7-20,58` — "Insights da IA Lovable" hardcoded citando colaborador fictício "João Silva"; contador "(12)" fixo, botão sem handler; montado em `pages/ESocialPage.tsx:25`.
- `admissoes/OnboardingDashboard.tsx:21-27` — `// Time to hire (mocked for demo)`.
- `metabase/MetabaseEmbed.tsx:60-90` — `FALLBACK_DATA` com headcount de 176 pessoas (real: 13).

**7. [ALTO] Domínio eSocial completo (22 componentes / 1.898 linhas) sobre tabelas 100% vazias.**
`esocial_eventos`=0, `configuracoes_esocial`=0, `certificados_digitais`=0, `esocial_transmissao_logs`=0 (`services/esocialService.ts:52,121,208,281,302,334`). Os 14 viewers S-xxxx (`ESocialEventViewer.tsx:1-13`) são renderizadores de `dados: any` sem qualquer submissão. `dashboard/MorningBriefing.tsx:68` faz `esocialTotal > 0 ? … : 100` → **"saúde eSocial" é sempre 100%** exatamente porque não há eventos.

**8. [MÉDIO] ~1.820 linhas de componentes órfãos, com 10 arquivos de teste dedicados a código morto.**
Órfãos confirmados (zero importador de produção, verificado com grep de JSX `<Nome`): 11 primitivos de `layout/` (885 linhas: `Stack`, `Flex`, `Container`, `Row`, `Column`, `Grid`, `SectionHeader`, `Breadcrumb`, `PageContainer`, `NavItem`, `EmpresaSwitcher`), `metabase/MetabaseEmbed.tsx` (423), `i18n/LanguageSelector.tsx` (76 — sem nenhuma referência sequer em teste), `ponto/AssinarEspelhoDialog.tsx` (176), `auth/LoginForm.tsx` (63), `ferias/FeriasSaldoReport.tsx` (85), `notifications/NotificationList.tsx` (33) e `NotificationItem.tsx` (33). Destes, 10 possuem teste próprio em `components/__tests__/` (`LoginForm.test.tsx`, `AssinarEspelhoDialog.test.tsx`, `NotificationList.test.tsx`, `NotificationItem.test.tsx`, `FeriasSaldoReport.test.tsx`, `PageContainer.test.tsx`, `Container.test.tsx`, `SectionHeader.test.tsx`, `LayoutPrimitives.test.tsx`, `LayoutGridSection.test.tsx`) — cobertura que não protege nenhum caminho de execução.

**9. [MÉDIO] Refactors abandonados com as duas versões presentes.**
- `components/auth/` (LoginForm, morto) × `components/login/` + `pages/LoginPage.tsx` (vivo).
- `components/admissao/` (2 arq.) × `components/admissoes/` (5 arq.) — a pasta antiga ainda é importada por `admissoes/DetalhesAdmissaoDialog.tsx:7` e `pages/ContratacaoPage.tsx:37`.
- `SignaturePad.tsx` duplicado em `components/admissao/` e `components/sst/`.
- `SectionHeader.tsx` duplicado em `components/layout/` (morto) e `components/dashboard/` (vivo).
- `esocial/`: `S1200Remuneracao.tsx`, `S1210Pagamentos.tsx`, `S2200Admissao.tsx`, `S2230Afastamento.tsx` são arquivos de **2 linhas** que só re-exportam de `Periodicos.tsx`/`NaoPeriodicos.tsx` (ex.: `S1200Remuneracao.tsx:1-2`) — consolidação iniciada e não concluída.
- Tabelas duplicadas de onboarding, **ambas com dado**: `onboarding_tarefas`=27 (`services/tabelas/rhService.ts:122`) × `tarefas_onboarding`=24 (`components/admissoes/OnboardingPageContent.tsx:34`).

**10. [MÉDIO] Módulo Exames lê a tabela errada.**
`pages/ExamesPage.tsx:7` monta os 4 componentes de `components/exames/` sobre `.from('exames')` = **0 linhas**, enquanto o dado real está em `exames_agendamentos`=6 e `asos`=6 — nenhuma das duas é consultada pela página. (`colaborador-detalhes/ASOTab.tsx` lê `asos` corretamente, o que confirma a existência do dado.)

**11. [BAIXO] Controles mortos espalhados pela UI.**
`premiacoes/RewardsSimulator.tsx:215` `onClick={() => {}}` ("Excluir snapshots"); `sst/SSTProgramasTab.tsx:78` botão "Renovar" sem handler; `sst/SSTIncidentesTab.tsx:81` "Exportar CAT" → `toast.info('… em breve')`; `portal/PortalMeusDadosTab.tsx:92` "Solicitar Alteração de Dados" sem handler; `settings/InlineTabs.tsx:322` "Webhook de Mudanças" sem handler; `settings/InlineTabs.tsx:302` badge "Conectado" do Bitrix24 hardcoded, não derivado da query de `integracoes` (0 linhas); `dashboard/AnalyticsSection.tsx:100-106` variável `passivoTrend` com série mock declarada e nunca usada.

---

## 4. Lacunas (não verificado)

1. **Comportamento em runtime** — `node_modules` ausente; não rodei build, typecheck, lint, testes nem o app. As falhas de query (achados 1 e 3) são deduzidas de comparação estática entre o código e o schema real de produção lido via MCP, não de execução observada. `NAO_VERIFICADO`.
2. **Se as tabelas `pcs_*` existem em algum ambiente não-produtivo** — só consultei o projeto `frjbfeamybqsejlvmqbl`. O `types.ts` gerado as declara, o que sugere outro banco de origem. `NAO_VERIFICADO`.
3. **RLS/permissões** — não avaliei se alguma tabela "vazia" na verdade tem linhas ocultas por policy para o papel usado pelo MCP. Usei `n_live_tup` de `pg_stat_user_tables` (estatística do planner, independente de RLS), o que mitiga mas não elimina a dúvida para tabelas recém-vacuumadas.
4. **Componentes que recebem dado por props a partir de Edge Functions** — a listagem de funções deployadas está indisponível (sem PAT), conforme o briefing. Componentes alimentados por `supabase.functions.invoke` (ex.: `folha/MetricasFolhaDashboard.tsx` → `folha-metrics`, `integracoes/ConfigPanels.tsx:65` → `sincronizar-bitrix`, `sst/GerarPgrButton.tsx`, `medidasDisciplinaresService` → `gerar-medida-disciplinar-pdf`) não puderam ter a camada de backend confirmada. `NAO_VERIFICADO`.
5. **Leitura integral dos 239 arquivos restantes** — 33 arquivos foram lidos por inteiro ou em blocos; os demais foram cobertos por varredura estática dirigida (referências, tabelas, marcadores de mock, handlers vazios). Componentes com lógica sutil não capturada por esses padrões podem ter escapado, sobretudo em `dashboard/AnalyticsSection.tsx` (1.010 linhas), `workflows/WorkflowDesigner.tsx` (637) e `ponto/PontoAdjustmentRequests.tsx` (471), dos quais li apenas trechos.
6. **`ui/` e `__tests__/`** — fora do escopo por instrução; as referências a testes acima vêm de listagem de nomes de arquivo, não de leitura do conteúdo dos testes (exceto os imports checados por grep).
